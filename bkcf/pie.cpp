#include <bits/stdc++.h>
using namespace std;

const int N=10e4+3;
int T;
int r[N];

int main(){
    freopen("bkcf/in.txt", "r", stdin);
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> T;
    while(T--){
        cin >> n >> f; f++;
        for(int i=1; i<=n; i++) cin >> r[i];

        
    }


    return 0;
}